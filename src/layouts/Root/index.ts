import styled from 'styled-components';
import { MediaQueries } from '../../styled-css/MediaQueries';

/**
 * Default page layout.
 * This should be the root element of an application
 */
export const Root = styled.div`
    ${MediaQueries}
`;
