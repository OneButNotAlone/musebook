import { Hidden, Overlay, Popover, Step, Tabs } from 'reakit';

// TODO: Containers should also export their child components using Object.assign pattern.
import { default as LoginContainer } from './LoginContainer';
import { default as PaginatorContainer } from './PaginatorContainer';

export default {
    Hidden,
    Overlay,
    Popover,
    Step,
    Tabs
};
